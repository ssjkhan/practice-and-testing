import path from "path";

export const __clientdir = path.resolve(
  __dirname,
  "..",
  "..",
  "client",
  "build",
);

export const __publicdir = path.resolve(
  __dirname,
  "..",
  "..",
  "client",
  "public",
);
